{
  "name": "inquimex_2025",
  "version": "1.0.0",
  "type": "module",
  "description": "WebSite Inquimex",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleanassets": "rimraf dist",
    "copyassets": "xcopy assets dist\\assets /s /e /q /y /i",
    "copycontacto": "xcopy contacto dist\\contacto /s /e /q /y /i",
    "build": "npm run cleanassets && npm run copyassets && npm run copycontacto && node copypages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpy": "^12.1.0"
  }
}
